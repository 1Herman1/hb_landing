---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class={`inline-flex px-3 py-1.5 bg-[rgba(244,244,241,0.07)] text-on-dark-muted text-sm rounded-lg font-medium border border-dark-line ${className}`}>
  <slot />
</span>
